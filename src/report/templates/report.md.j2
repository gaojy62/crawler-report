# {{ report_name }} - {{ generated_date }}

> 生成时间：{{ generated_at }}
{% if ai_stats %}
> AI 消耗：{{ ai_stats.total_input_tokens + ai_stats.total_output_tokens }} tokens (输入 {{ ai_stats.total_input_tokens }} / 输出 {{ ai_stats.total_output_tokens }}){% if ai_stats.total_cost_usd > 0 %}，费用 ${{ "%.6f"|format(ai_stats.total_cost_usd) }} USD{% endif %}
{% endif %}
> 数据来源：{{ sources | join('、') }}

---

## 今日 Top {{ item_count }}

{% for item in items %}
### {{ loop.index }}. {{ item.title }}

| 属性 | 内容 |
|------|------|
| **来源** | {{ item.source_name }} |
| **时间** | {{ item.published_at or "未知" }} |
| **分类** | {{ item.category }} |
| **评分** | {{ item.score }}/10 |

**背景**：{{ item.background or "暂无背景信息" }}

**影响**：{{ item.impact or "暂无影响分析" }}

> 原文链接：[{{ item.url | truncate(60) }}]({{ item.url }})

---

{% endfor %}

## 相关链接

{% for item in items %}
- [{{ item.source_name }} - {{ item.title | truncate(40) }}]({{ item.url }})
{% endfor %}